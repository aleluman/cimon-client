import { styled } from "@/shared/constants/stitches";

export const Label = styled("label", {
  display: "flex",
  gap: "0.4rem",
  fontWeight: 500,
  fontSize: "0.8rem",
  flexDirection: "column",
  color: "$textNormal",
});
